# Lido pelo Vite com --mode easepanel (npm run build:easepanel)
# VITE_REALTIME_URL bypassa Kong → vai direto ao container Realtime (porta 4000)
# Necessário: Traefik EasyPanel nao faz upgrade WSS via Kong
VITE_REALTIME_URL=wss://realtime.teusite.top/socket
